@import '../../../../styles/variables.styl'

.subscribeForm
  display flex
  justify-content center
  flex-wrap wrap
  .input, .button
    margin 30px 0
    width 100%
    max-width 500px
    &:last-child
      margin-bottom 0
    @media screen and (min-width mediumWidth) {
      width max-content
      margin 30px 15px 0
    }